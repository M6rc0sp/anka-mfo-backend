# Environment
NODE_ENV=development

# API
API_PORT=3333

# Database (Docker: use 'postgres' as DB_HOST, Local: use 'localhost')
DB_HOST=postgres
DB_PORT=5432
DB_NAME=anka
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=dev-secret-key-change-in-production
